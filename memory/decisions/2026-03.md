# 2026年03月

*   **OpenClaw Browser デバッグと修復:**
    *   `gateway closed (1000 normal closure)` 問題のデバッグと解決。
    *   `token_mismatch` / `device_token_mismatch` の解決、および `openclaw-control-ui webchat` の接続回復。
    *   CLI側の `handshake timeout` 問題（クライアント実装/配布差分が原因）の特定と、2026.3.14版へのアップデートによる問題解決。
    *   結果として、eBayのAI側ブラウザ操作が復旧。

*   **まいちゃんの誕生日決定:**
    *   毎年お祝いする前提で、まいちゃんの誕生日を **3月20日**に決定。

*   **運用ルール（とんすけより）:**
    *   権限問題でコマンドが通らない場合は、`sudo` をまず試す。
    *   昇格実行が必要な操作は、通常権限実行→失敗時に `sudo` で再試行を標準手順にする。
