# 2026-03-22

- とんすけが「昨日の続き」を再開。OpenClaw browser の `gateway closed (1000 normal closure)` 問題を一緒にデバッグ中。
- 現在判明していること:
  - `openclaw config get gateway --json` は正常（`auth.mode: none`）。
  - `env | grep -E 'OPENCLAW|GATEWAY'` は空で、シェル環境変数汚染は見えない。
  - それでも `openclaw gateway status` で `gateway url override requires explicit credentials` が出ることがある。
  - `~/.openclaw` 配下のセッション履歴に大量ノイズがあり、実設定の切り分けを優先。
- 進め方の合意:
  - とんすけ実行で「1行ずつコマンド指示」で進める。
  - 次は設定実体（`~/.openclaw/openclaw.json`, `~/.openclaw/agents/main/agent`, `~/.config/systemd/user/openclaw-gateway.service`）だけを grep 対象にして override 設定を特定する方針。
- 追加で判明した重要点（09:17〜09:25 JST）:
  - `openclaw gateway probe --json` で `connect failed: device identity required`（code 1008）が出た。
  - `openclaw qr --json` は `Gateway auth is not configured (no token or password).` となり、`auth:none` では現行フローと不整合があることを確認。
  - `~/.openclaw/openclaw.json` を `gateway.auth.mode=token` に変更し、ランダムtoken発行後に gateway 再起動。`openclaw gateway status` と `RPC probe: ok` は回復。
  - その後 `openclaw browser status` は依然 `gateway closed (1000 normal closure)`。
  - ログで根因候補が明確化: `gateway/ws unauthorized ... reason=token_mismatch`（`openclaw-control-ui webchat` が古いtokenで接続）。
  - `openclaw browser --help` で、`--url/--token` はサブコマンド後ではなく親コマンド位置に置くのが正しい可能性を確認。
- 次アクション（未実行の最新1行）:
  - `openclaw browser --url ws://127.0.0.1:18789 --token <最新token> status` で親コマンド位置オプションを検証する。

- その後の解決ログ（10:30〜22:00 JST）:
  - `openclaw-control-ui` 側は `token_mismatch` / `device_token_mismatch` を踏んでいたが、再ペアリング・設定修復で `webchat connected` まで回復。
  - 長く詰まっていた本丸は「CLI側のみ handshake timeout → gateway closed (1000)」で、認証不足ではなくクライアント実装/配布差分が原因だった。
  - 動作PCとの差分比較で決定打: 動作PCは npm グローバル版ではなく `~/.local/bin/openclaw` から repo build版（OpenClaw 2026.3.14）を使用。
  - 問題PCでは npm `latest` が 2026.3.13 のため更新しても改善せず。`/home/tomoyuki/openclaw` を git install/build し、`~/.local/bin/openclaw` ラッパーを作成して 2026.3.14 (c70ae1c) に切替。
  - 切替後、`openclaw browser ... tabs` が正常応答（`No tabs`）し、1000エラーを脱出。
  - そのまま実操作確認で eBay を開くことに成功。AI側ブラウザ操作の復旧を確認。

- 運用上の重要メモ:
  - OpenClawの昇格実行は送信元プロバイダの allowFrom に依存。Telegram（8719386273）経由では再起動実行できたが、webchat 経由は拒否されるケースあり。
  - 類似障害時は「同じ構成で動く端末との `which openclaw` / 実体パス差分」を最優先で確認すると早い。

- eBay操作セッション結果:
  - Ship to は米国へ変更成功（ヘッダーで `お届け先: 米国` を確認）。
  - 言語は `?_lang=en-US` 付与でもUIラベル日本語表示が残る挙動あり（eBay側ロケール/表示仕様の可能性）。実操作は継続可能。
